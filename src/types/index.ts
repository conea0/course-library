type problem = {
	text:string,
	testcase:Testcase,
	code:string,
}

type Testcase = {
    input: string[],
	output: string[],
}

const getProblem = (id: Number): problem => {
	return(
        
    );
}